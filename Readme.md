     <!-- git --version
      git init -->
   <!-- git remote add origin https://github.com/roshangupta956947/demo.git -->

   <!-- git remote -v -->
<!-- username& email 
if we are to store configuration globally
git config --global user.name "Roshan Gupta"
git config --global user.email "roshangupta94291@gmail.com" -->
<!-- to see the configuration
git config --list -->

<!-- 
to see current status
git status

add the current directory to see the git
git add . -->

<!-- git commit -m "First Initialization" -->
branch find ke liye => git branch
 <!-- to rename the branch
git push -u main

git push -u origin main


1. git diff